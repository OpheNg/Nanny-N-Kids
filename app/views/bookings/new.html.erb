 <div class="container-booking">

    <div class="nanny-choice">
        <h3>Your super nanny</h3>
        <p class="price"> PRICE PER DAY : <b> <%= @nanny.price_per_day %> £</b></p>
        <hr>
        <p>NANNY n°: <b><%= @nanny.id %></b></p>
        <hr>
        <p> NAME : <b><%= @nanny.first_name %></b>  <b><%=  @nanny.last_name %></b></p></p>
        <hr>
        <%# <p>LAST NAME :
        <hr> %>
    </div>

      <div class="form-info">
        <h3 class="booking-title">Book Here!</h3>

          <%= simple_form_for [@nanny, @booking] do |f| %>

          <%= f.input :start_date, error: 'S is mandatory, please specify one' %>
          <%= f.input :end_date %>
          <%= f.input :nanny_id, input_html: {value: params[:nanny_id]} %>
          <%= f.button :submit %>

        <% end %>
     </div>
  </div>
