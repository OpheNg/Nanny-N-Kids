<h1> Edit your Booking </h1>

<h2> Booking selected : </h2>
<ul>
    <li>Booking n°: <b><%= @booking.id %></b></li>
    <li>Start date : <b><%= @booking.start_date %></b></li>
    <li>End date : <b><%= @booking.end_date %></b></li>
    <li>Nanny's name : <b><%= @booking.nanny.first_name %> <%= @booking.nanny.last_name %></b></li>
    <li>Nanny n°: <b><%= @booking.nanny.id %></b></li>
</ul>

<h3>Please update the info by filling this form</h3>

<%# remmplacer @user par @nanny, faut changer toutes les routes pour le create et le new %>
<%= simple_form_for [@nanny, @booking] do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.button :submit %>
<% end %>
<%= link_to "Back to index", bookings_path %>
