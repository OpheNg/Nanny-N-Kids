<h1>All the Nannies :</h1>
<br>
<ul>
<br>
  <% @nannies.each do |nanny| %>
  <br>
    <li>created by user id : <%= nanny.user.id %>
    <li>nanny id : <%= nanny.id %></strong></li>
    <li>Nanny first name : <%= nanny.first_name %></li>
    <li>Nanny last name : <%= nanny.last_name %></li>
    <li>Price per day : <%= nanny.price_per_day %>
    <% if nanny.user == current_user %>
    <li><%= link_to "Edit this Nanny", edit_nanny_path(nanny.user.id, nanny.id) %></li>
    <li><%= link_to "❌ Delete", nanny_path(nanny.user.id, nanny.id), method: :delete, data: { confirm: "Are you sure?" } %></li>
    <% else %>
    <li><%= link_to "👉 Book this Nanny", new_nanny_booking_path(nanny) %></li>
    <% end %>
    </li>
  </div>
    <% end %>

 